# Test Data

Temporary test images, bags, or sample data can be placed here on the D3G board.

Do not commit test data. Use `TEST_DATA_URL` with `scripts/download_test_models.sh`
when the team decides on a shared dataset URL.
